import Servers from "@/components/servers/Servers";
import React from "react";
import { ScrollView } from "react-native";

function ServersSelect() {
  return (
    <ScrollView>
      <Servers />
    </ScrollView>
  );
}

export default ServersSelect;
